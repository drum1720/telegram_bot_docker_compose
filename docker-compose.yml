version: '3.8'fghgfghfghfvolumes:
  postgres_data:
services:
  telegramm-bot-listener:
    restart: always
    build: ./TelegramBotListener/
    depends_on:
      - rifma-search
      - image-downloader
      - image-handler
  rifma-search:
    restart: always
    build: ./rifmaSearch/54645646
    depends_on:
      - db
  tor-search:
    restart: always
    build: ./torrent_search/
  image-downloader:
    restart: always
    build: ./pixabayImageD546456ownloader/
  image-handler:
    restart: always
    build: ./imageHandler/
  db:
    restart: always
    image: postgres:latest
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=5464564
      - POSTGRES_USER=4564564
      - POSTGRES_DB=rifmaapi646456
